版本1（只对ext2分区有效）

编译：
		命令行下进入recover目录，直接make即可生成可执行文件recover

工作流程及原理：
		1.鉴于对分区操作需要根用户权限，所以在命令行中执行软件请用sudo提升权限
		2.软件执行后输入分区路径（必须是ext2分区），软件会提取分区信息
		3.遍历所有inode节点，找出被删除的inode节点（空闲且i_dtime不为空）
		4.根据索引数组恢复在直接块中的数据。并存与/home/目录下，文件名为“inode“+inode号（回复的文件数据最多只有12个块大）


